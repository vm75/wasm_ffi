name: example
description: "A dart-web example for wasm_ffi"
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  web: ^1.1.1
  wasm_ffi:
    path: ../

dev_dependencies:
  build_runner: ^2.11.0
  build_web_compilers: ^4.4.10
  ffigen: ^20.1.1
  lints: ^6.1.0
